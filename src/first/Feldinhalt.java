package first;

public class Feldinhalt {
    Fraction fraction;

    public Fraction getFraction() {
        if (/*Fieldstate Fraction*/) {
            return fraction;
        }
        return Fraction.ZERO;
    }
}
